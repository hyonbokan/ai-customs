# Deny-by-default egress proxy for the agent pod. Only hosts matching the filter are reachable;
# everything else is refused, so an LLM-authored Bash script cannot exfiltrate document data to an
# arbitrary host. HTTPS is tunneled via CONNECT (restricted to 443), filtered on the target host.
Port 8888
Listen 0.0.0.0
Timeout 600

Allow 0.0.0.0/0

Filter "/etc/tinyproxy/filter"
FilterDefaultDeny Yes
FilterExtended On
FilterURLs Off

ConnectPort 443
